run the following docker command to start the application in the docker container:

$ docker run -d -p 2400:80 raminarmanfar/bowl-scoring

After creation of the container open the browser with the link:

http://localhost:2400
